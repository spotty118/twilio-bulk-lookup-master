#!/usr/bin/env bash

# Exit on failure
set -e

# Check if foreman is installed
if ! command -v foreman &> /dev/null; then
  echo "Foreman is not installed. Installing foreman..."
  gem install foreman
fi

# Run the development processes defined in Procfile.dev
exec foreman start -f Procfile.dev
